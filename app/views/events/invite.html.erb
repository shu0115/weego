<!DOCTYPE HTML>
<html lang="ja">
  <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1, user-scalable=no" >
  <meta name="description" content="やiPhoneで簡単に、買ったその場で家計簿をつけられるサービス「Receibo（レシーボ）」" />
  <meta name="keywords" content="iPhone, スマートフォン, 家計簿, レシーボ, Receibo, ハッカソン, Ruby, Ruby on Rails" />
  <link rel="stylesheet" href="/assets/screen.css" type="text/css" media="screen" title="no title" charset="utf-8">
<head>
	<meta charset="UTF-8">
	<title>Weego</title>
</head>
<body class="member">
  <header>
  <div class="button-back"><a href="#">戻る</a></div>
  <div class="logo"><img src="/assets/logo_header.png" alt="Weego" width="320px" height="30px"></div>
  </header>
  
  <article>
    <%= form_tag( action: "complete", event_id: @event.id ) do %>
    <!--<form>-->
      <ul>
        <% 1.upto(3){ |index| %>
        <li>
        <div>
          <div class="icon"><img src="/assets/member_icon.png" width="32" height="32"></div>
          <p class="user-name">ツイッターユーザー名<%= index %></p>
          <%#= hidden_field_tag "add[ids][#{index}][name]", "名前#{index}"  %>
          <p class="account-name">@account_<%= index %></p>
          <!--<p class="checkbox"><input type="checkbox"> </p>-->
          <p class="checkbox"><input id="add_ids_<%= index %>" name="add[ids][<%= index %>]" type="checkbox" value='<%= "account_#{index}" %>' /></p>
        </div>
        </li>
        <% } %>
      </ul>
      <div class="button-area login">
        <!--<input type="submit" name="commit" value="誰と？" class="button-submit">-->
	      <%= submit_tag "誰と？", class: "button-submit" %>
      </div>
    <!--</form>-->
    <% end %>
  </article>
  <footer></footer>
</body>
</html>
